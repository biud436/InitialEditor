{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Electron",
            "appDir": "${workspaceFolder}",

            "sourceMaps": true,
            "preLaunchTask": "Build Webpack",
            "cwd": "${workspaceRoot}"
        }
    ],
    "compounds": [
        {
            "name": "Build and Run",
            "configurations": ["Launch Electron"]
        }
    ]
}
